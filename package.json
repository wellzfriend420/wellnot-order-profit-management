{
  "name": "wellnot-order-profit-management",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check public/app.js && node --test",
    "verify:pdf": "node tools/render-reports.mjs"
  },
  "engines": {
    "node": ">=22.5"
  },
  "devDependencies": {
    "playwright-core": "^1.61.1"
  },
  "dependencies": {
    "exceljs": "^4.4.0"
  }
}
